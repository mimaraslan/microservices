---
server:
  port: 8888           
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mimaraslan/spring-cloud-config-server1 # default
          search-paths:
            - 'configation/{application}'